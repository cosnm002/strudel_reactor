//Play button
export const handlePlay = (editor) => {
    editor.evaluate();
}

//Stop button
export const handleStop = (editor) => {
    editor.stop();
}

//Process and Play button
export const procAndPlay = (editor) => {
    editor.evaluate();

}